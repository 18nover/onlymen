/**
 * Update Settings Action
 *
 * Extracts and saves setting values from natural language user messages.
 * Uses LLM to parse user responses and map them to settings.
 */
import type { Action } from "../../../types/index.js";
/**
 * SECRETS_UPDATE_SETTINGS action - extracts and saves setup settings from natural language.
 */
export declare const updateSettingsAction: Action;
//# sourceMappingURL=action.d.ts.map