/**
 * Pending pairing handshakes — per-channel sender requests (with code and
 * last-seen timestamp) awaiting approval; backs the runtime pairing service.
 */
import type { SchemaTable } from "../types/schema.js";
/**
 * Abstract schema for the pairing_requests table.
 * Has multiple unique indexes.
 */
export declare const pairingRequestSchema: SchemaTable;
//# sourceMappingURL=pairing-request.d.ts.map